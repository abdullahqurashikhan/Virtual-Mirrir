import 'package:flutter/material.dart';

class TryOnScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Try-On")),
      body: Center(child: Text("Try-On Area")),
    );
  }
}
